# zad2-systemy-operacyjne

W celu zainstalowania i uruchomienia programu należy wpisać komendy:

autoreconf --install
./configure
make
sudo make install
systemy

W celu odinstalowania należy wpisać komendę:

sudo make uninstall
